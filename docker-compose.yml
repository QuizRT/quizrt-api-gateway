--- 
services: 
  consul: 
    image: consul
    restart: always
  gateway: 
    build: 
      context: ./Gateway
    depends_on: 
      - consul
      - registrator
    network_mode: host
  registrator: 
    command: "-internal consul://consul:8500"
    image: gliderlabs/registrator
    network_mode: host
    restart: always
    volumes: 
      - "/var/run/docker.sock:/tmp/docker.sock"
version: "3"
